<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TripBuddy">

        <!-- Activities without intent-filters don't need android:exported -->
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ViewGalleryClass" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".LoginScreen" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".PlanTrip" />
        <activity android:name=".WelcomeMessage" />
        <activity android:name=".MainActivity" />

        <!-- Launcher / splash -->
        <activity
            android:name=".ImageSplash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
